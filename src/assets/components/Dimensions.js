import { StyleSheet, Text, View } from 'react-native'
import React,{Dimension} from 'react'

const Dimensions = () => {
    const { deviceWidth, deviceHeight } = Dimensions.get('window');
 
}

export default Dimensions

const styles = StyleSheet.create({})